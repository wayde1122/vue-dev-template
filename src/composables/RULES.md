# src/composables/ 规则

## 定位

组合式函数（composables）用于复用“有状态或带副作用的逻辑”（如通知、鉴权、任务操作）。

## 规则

- **命名**：统一 `useXxx`
- **输入输出明确**：返回值尽量是稳定对象（避免每次调用返回新引用导致不必要渲染）
- **副作用隔离**：订阅/监听器要提供清理（在组件卸载时能正确释放）
- **并发处理**：允许部分失败时用 `Promise.allSettled()`
- **默认值**：使用 `??` / `??=`
