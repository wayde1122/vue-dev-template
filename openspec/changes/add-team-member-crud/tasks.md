## 1. 规范与设计

- [x] 1.1 确认范围：数据来源（mock）、身份证展示（不脱敏）、字段校验（身份证唯一、枚举字段）
- [x] 1.2 完善 `design.md` 中的关键决策（数据模型、枚举、唯一性、交互）

## 2. 实施

- [x] 2.1 新增人员数据模型（类型定义）与基础校验工具函数（身份证唯一、枚举校验）
- [x] 2.2 新增 `team`/`member` 相关 store（CRUD + mock 数据初始化）
- [x] 2.3 将 `/team` 页面从占位页改造为"人员管理"页面（表格 + 工具栏）
- [x] 2.4 实现新增/编辑表单（弹窗，含必填与错误提示）
- [x] 2.5 实现删除确认流程（避免误删）
- [x] 2.6 单元测试：store 的 CRUD、唯一性校验、枚举校验

## 3. 验证

- [x] 3.1 `npm run type-check`
- [x] 3.2 `npm run lint`
- [x] 3.3 `npm run test:run`
- [ ] 3.4 手动验收：新增/编辑/删除功能正常工作
